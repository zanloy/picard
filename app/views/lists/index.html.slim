h1
  - if is_admin?
    .pull-right
      = button_to 'Add New', new_list_path, { method: :get, class: 'btn btn-primary' }
  ' Lists

table
  thead
    tr
      th Name
      th Description
      th &nbsp;
  tbody
    - @lists.each do |list|
      tr
        td = link_to list.name, list
        td
          = list.description
        td
          = link_to 'Edit', edit_list_path(list)
          = link_to 'Destroy', list, data: {:confirm => 'Are you sure?'}, :method => :delete
